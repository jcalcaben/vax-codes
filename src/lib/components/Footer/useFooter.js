import {
  helpfulLinks,
  qrCodes,
  adminPortal,
  otherLinks,
} from "../../data/footerLinks";

const useFooter = (props) => {
  return {
    helpfulLinks,
    qrCodes,
    adminPortal,
    otherLinks,
  };
};

export default useFooter;
